// Paste your own keys here, save, and reload the site.
const CONFIG = {
  // 1) OS Data Hub → your project → OS Maps API → "Project API Key"
  osKey: "PASTE_YOUR_OS_MAPS_API_KEY_HERE",

  // 2) Firebase console → Project settings → Your apps → Web app → "SDK setup and configuration" → Config
  firebase: {
    apiKey: "PASTE",
    authDomain: "PASTE.firebaseapp.com",
    projectId: "PASTE",
    storageBucket: "PASTE.firebasestorage.app",
    messagingSenderId: "PASTE",
    appId: "PASTE"
  }
};
